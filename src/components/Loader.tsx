const Loader = () => {
  return (
    <div className="fixed flex items-center justify-center place-items-center  w-full h-screen  bg-white z-40 transition-all duration-300">
      <div className="rounded-full border-t-4 border-primaryColor w-16 h-16 animate-spin"></div>
    </div>
  );
};

export default Loader;
